<UserControl xmlns="https://github.com/avaloniaui" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:i="clr-namespace:Avalonia.Xaml.Interactivity;assembly=Avalonia.Xaml.Interactivity" 
  xmlns:behaviors="clr-namespace:WalletWasabi.Gui.Behaviors;assembly=WalletWasabi.Gui" 
  xmlns:local="clr-namespace:WalletWasabi.Gui.Controls.WalletExplorer;assembly=WalletWasabi.Gui" 
  xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui" 
  xmlns:converters="clr-namespace:WalletWasabi.Gui.Converters;assembly=WalletWasabi.Gui" x:Class="WalletWasabi.Gui.Controls.WalletExplorer.BuildTransactionDetails">
  <UserControl.Resources>
    <converters:BooleanStringConverter x:Key="BooleanStringConverter" />
    <converters:LurkingWifeModeStringConverter x:Key="LurkingWifeModeStringConverter" />
    <converters:MoneyBrushConverter x:Key="MoneyBrushConverter" />
  </UserControl.Resources>
  <DockPanel LastChildFill="True">
    <StackPanel DockPanel.Dock="Top">
      <Grid ColumnDefinitions="170, *">
        <TextBlock Classes="monospaceFont" Text="Transaction Id:" />
        <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding TransactionId, ConverterParameter=50, Converter={StaticResource LurkingWifeModeStringConverter}, Mode=OneWay}" />
      </Grid>
      <Grid ColumnDefinitions="170, *">
        <TextBlock Classes="monospaceFont" Text="Input Amount (BTC):" />
        <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding TotalInputValue}" Foreground="{Binding TotalInputValue, Converter={StaticResource MoneyBrushConverter}}" />
      </Grid>
      <Grid ColumnDefinitions="170, *">
        <TextBlock Classes="monospaceFont" Text="Output Amount (BTC):" />
        <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding TotalOutputValue}" Foreground="{Binding TotalOutputValue, Converter={StaticResource MoneyBrushConverter}}" />
      </Grid>
      <Grid ColumnDefinitions="170, *">
        <TextBlock Classes="monospaceFont" Text="Transaction Date:" />
        <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding Path=DateTime, StringFormat=\{0:yyyy-MM-dd HH:mm\}}" />
      </Grid>
    </StackPanel>
    <controls:ExtendedListBox Items="{Binding CombinedTXOInfo}">
      <controls:ExtendedListBox.DataTemplates>
        <DataTemplate DataType="local:BuildTransactionDetailsCoinViewHeader">
          <Grid ColumnDefinitions="*,*">
            <Border Grid.Column="0" Padding="7" BorderThickness="1" BorderBrush="{DynamicResource ApplicationAccentForegroundBrush}">
              <TextBlock Classes="FontSizeNormal" Text="Inputs" Foreground="White" />
            </Border>
            <Border Grid.Column="1" Padding="7" BorderThickness="1" BorderBrush="{DynamicResource ApplicationAccentForegroundBrush}">
              <TextBlock Classes="FontSizeNormal" Text="Outputs" Foreground="White" />
            </Border>
          </Grid>
        </DataTemplate>
        <DataTemplate DataType="local:InOutInfoTuple">
          <Grid ColumnDefinitions="*,*">
            <Border Padding="1" Grid.Column="0" Background="DarkGreen" BorderBrush="Green" HorizontalAlignment="Left" BorderThickness="1" CornerRadius="6">
              <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding Input.Address}" Foreground="White" />
            </Border>
            <Border Padding="1" Grid.Column="1" Background="DarkGreen" BorderBrush="Green" HorizontalAlignment="Left" BorderThickness="1" CornerRadius="6">
              <controls:ExtendedTextBox Grid.Column="1" Classes="selectableTextBlock monospaceFont Transparent" Text="{Binding Output.Address}" Foreground="White" />
            </Border>
          </Grid>
        </DataTemplate>
      </controls:ExtendedListBox.DataTemplates>
    </controls:ExtendedListBox>
  </DockPanel>
</UserControl>
