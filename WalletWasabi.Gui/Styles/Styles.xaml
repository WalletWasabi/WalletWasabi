<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dock="clr-namespace:Dock.Avalonia.Controls;assembly=Dock.Avalonia"
        xmlns:shell="clr-namespace:AvalonStudio.Shell.Controls;assembly=AvalonStudio.Shell"
        xmlns:converters="clr-namespace:WalletWasabi.Gui.Converters;assembly=WalletWasabi.Gui"
        xmlns:behaviors="clr-namespace:WalletWasabi.Gui.Behaviors;assembly=WalletWasabi.Gui"
        xmlns:shellext="clr-namespace:AvalonStudio.Controls;assembly=AvalonStudio.Shell.Extensibility"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui">
  <Style>
    <Style.Resources>
      <SolidColorBrush x:Key="ThemeAccentBrush">#007ACC</SolidColorBrush>
      <SolidColorBrush x:Key="HyperLinkForeground">#00A2E8</SolidColorBrush>
      <SolidColorBrush x:Key="ErrorBrush">#F25022</SolidColorBrush>
      <SolidColorBrush x:Key="WarningMessageForeground">#FFDD00</SolidColorBrush>
      <SolidColorBrush x:Key="GreenBrush">#00DD10</SolidColorBrush>

      <SolidColorBrush x:Key="NotificationCardBackgroundBrush" Color="#444444" Opacity="0.75" />
      <SolidColorBrush x:Key="NotificationCardInformationBackgroundBrush" Color="#007ACC" Opacity="0.75" />
      <SolidColorBrush x:Key="NotificationCardSuccessBackgroundBrush" Color="#1F9E45" Opacity="0.75" />
      <SolidColorBrush x:Key="NotificationCardWarningBackgroundBrush" Color="#FDB328" Opacity="0.75" />
      <SolidColorBrush x:Key="NotificationCardErrorBackgroundBrush" Color="#BD202C" Opacity="0.75" />

      <sys:Double x:Key="FontSizeSmall">12</sys:Double>
      <sys:Double x:Key="FontSizeNormal">14</sys:Double>
      <sys:Double x:Key="FontSizeLarge">16</sys:Double>
      <FontFamily x:Key="MonospacedFont">avares://WalletWasabi.Gui/Assets#Cascadia Mono</FontFamily>
      <converters:ErrorSeverityColorConverter x:Key="ErrorSeverityColorConverter" />
      <converters:ErrorDescriptorToBorderColorConverter x:Key="ErrorDescriptorToBorderColorConverter" />

      <Thickness x:Key="PageRootPadding">15 0 30 0</Thickness>

      <!-- TODO: Remove this when we switch to fluent theme. -->
      <StreamGeometry x:Key="PasswordBoxRevealButtonData">m10.051 7.0032c2.215 0 4.0105 1.7901 4.0105 3.9984s-1.7956 3.9984-4.0105 3.9984c-2.215 0-4.0105-1.7901-4.0105-3.9984s1.7956-3.9984 4.0105-3.9984zm0 1.4994c-1.3844 0-2.5066 1.1188-2.5066 2.499s1.1222 2.499 2.5066 2.499 2.5066-1.1188 2.5066-2.499-1.1222-2.499-2.5066-2.499zm0-5.0026c4.6257 0 8.6188 3.1487 9.7267 7.5613 0.10085 0.40165-0.14399 0.80877-0.54686 0.90931-0.40288 0.10054-0.81122-0.14355-0.91208-0.54521-0.94136-3.7492-4.3361-6.4261-8.2678-6.4261-3.9334 0-7.3292 2.6792-8.2689 6.4306-0.10063 0.40171-0.50884 0.64603-0.91177 0.54571s-0.648-0.5073-0.54737-0.90901c1.106-4.4152 5.1003-7.5667 9.728-7.5667z</StreamGeometry>
      <!-- TODO: Remove this when we switch to fluent theme. -->
      <StreamGeometry x:Key="PasswordBoxHideButtonData">m0.21967 0.21965c-0.26627 0.26627-0.29047 0.68293-0.07262 0.97654l0.07262 0.08412 4.0346 4.0346c-1.922 1.3495-3.3585 3.365-3.9554 5.7495-0.10058 0.4018 0.14362 0.8091 0.54543 0.9097 0.40182 0.1005 0.80909-0.1436 0.90968-0.5455 0.52947-2.1151 1.8371-3.8891 3.5802-5.0341l1.8096 1.8098c-0.70751 0.7215-1.1438 1.71-1.1438 2.8003 0 2.2092 1.7909 4 4 4 1.0904 0 2.0788-0.4363 2.8004-1.1438l5.9193 5.9195c0.2929 0.2929 0.7677 0.2929 1.0606 0 0.2663-0.2662 0.2905-0.6829 0.0726-0.9765l-0.0726-0.0841-6.1135-6.1142 0.0012-0.0015-1.2001-1.1979-2.8699-2.8693 2e-3 -8e-4 -2.8812-2.8782 0.0012-0.0018-1.1333-1.1305-4.3064-4.3058c-0.29289-0.29289-0.76777-0.29289-1.0607 0zm7.9844 9.0458 3.5351 3.5351c-0.45 0.4358-1.0633 0.704-1.7392 0.704-1.3807 0-2.5-1.1193-2.5-2.5 0-0.6759 0.26824-1.2892 0.7041-1.7391zm1.7959-5.7655c-1.0003 0-1.9709 0.14807-2.8889 0.425l1.237 1.2362c0.5358-0.10587 1.0883-0.16119 1.6519-0.16119 3.9231 0 7.3099 2.6803 8.2471 6.4332 0.1004 0.4018 0.5075 0.6462 0.9094 0.5459 0.4019-0.1004 0.6463-0.5075 0.5459-0.9094-1.103-4.417-5.0869-7.5697-9.7024-7.5697zm0.1947 3.5093 3.8013 3.8007c-0.1018-2.0569-1.7488-3.7024-3.8013-3.8007z</StreamGeometry>
    </Style.Resources>
  </Style>

  <Style Selector="shellext|DocumentTabControl /template/ TabStripItem">
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>

  <Style Selector="NotificationCard/template/ ContentControl#PART_Content">
    <Setter Property="MinHeight" Value="50" />
  </Style>

  <Style Selector="shell|MetroWindow /template/ Button#minimiseButton">
    <Setter Property="Content">
      <Template>
        <Panel Margin="4" Height="8" Width="8">
          <DrawingPresenter>
            <DrawingGroup>
              <DrawingGroup.Children>
                <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
                <GeometryDrawing Brush="{DynamicResource ThemeForegroundBrush}" Geometry="F1M15,15L1,15 1,10 15,10z" />
                <GeometryDrawing Brush="{DynamicResource ThemeForegroundBrush}" Geometry="F1M14,11L2,11 2,14 14,14z" />
              </DrawingGroup.Children>
            </DrawingGroup>
          </DrawingPresenter>
        </Panel>
      </Template>
    </Setter>
  </Style>
  <Style Selector="shell|MetroWindow /template/ Button#restoreButton">
    <Setter Property="Content">
      <Template>
        <Panel Margin="4">
          <Path Height="8" Width="8" Stretch="Uniform" UseLayoutRounding="False" Fill="{DynamicResource ThemeForegroundBrush}" Data="M4,4H20V20H4V4M6,8V18H18V8H6Z" />
        </Panel>
      </Template>
    </Setter>
  </Style>
  <Style Selector="shell|MetroWindow:maximised /template/ Button#restoreButton">
    <Setter Property="Content">
      <Template>
        <Panel Margin="4">
          <Path Height="8"
                Width="8"
                Stretch="Uniform"
                UseLayoutRounding="False"
                Fill="{DynamicResource ThemeForegroundBrush}"
                Data="M4,8H8V4H20V16H16V20H4V8M16,8V14H18V6H10V8H16M6,12V18H14V12H6Z" />
        </Panel>
      </Template>
    </Setter>
  </Style>
  <Style Selector="shell|MetroWindow /template/ Button#closeButton">
    <Setter Property="Content">
      <Template>
        <Panel Margin="4">
          <Path
            Height="8"
            Width="8"
            Stretch="Uniform"
            UseLayoutRounding="False"
            Data="M13.46,12L19,17.54V19H17.54L12,13.46L6.46,19H5V17.54L10.54,12L5,6.46V5H6.46L12,10.54L17.54,5H19V6.46L13.46,12Z"
            Fill="{DynamicResource ThemeForegroundBrush}" />
        </Panel>
      </Template>
    </Setter>
  </Style>

  <!-- TODO: Remove this when we switch to fluent theme. -->
  <Style Selector="TextBox.revealPasswordButton[AcceptsReturn=False][IsReadOnly=False]:not(TextBox:empty)">
    <Setter Property="InnerRightContent">
      <Template>
        <Panel>
          <Panel.Styles>
            <Style Selector="ToggleButton[IsChecked=True]">
              <Setter Property="(ToolTip.Tip)" Value="Hide Password" />
            </Style>
            <Style Selector="ToggleButton[IsChecked=False]">
              <Setter Property="(ToolTip.Tip)" Value="Show Password" />
            </Style>
          </Panel.Styles>
          <ToggleButton Classes="PasswordBoxRevealButton" Background="Transparent" Cursor="Arrow" IsChecked="{Binding $parent[TextBox].RevealPassword, Mode=TwoWay}" />
        </Panel>
      </Template>
    </Setter>
  </Style>
  
  <!-- TODO: Remove this when we switch to fluent theme. -->
  <Style Selector="TextBox.revealPasswordButton">
    <Setter Property="PasswordChar" Value="*" />
  </Style>

  <!-- TODO: Remove this when we switch to fluent theme. -->
  <Style Selector="ToggleButton.PasswordBoxRevealButton">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="PART_ButtonLayoutBorder"
                  Background="Transparent"
                  Width="12">
            <Panel>
              <Path x:Name="PART_GlyphElement_Reveal"
                    Fill="White"
                    Data="{DynamicResource PasswordBoxRevealButtonData}"
                    Height="8"
                    Width="12"
                    Stretch="Uniform"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
              <Path x:Name="PART_GlyphElement_Hide"
                    Fill="White"
                    Data="{DynamicResource PasswordBoxHideButtonData}"
                    Height="12"
                    Width="12"
                    Stretch="Uniform"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
            </Panel>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- TODO: Remove this when we switch to fluent theme. -->
  <Style Selector="ToggleButton.PasswordBoxRevealButton:not(ToggleButton:checked) /template/ Path#PART_GlyphElement_Hide">
    <Setter Property="IsVisible" Value="False" />
  </Style>

  <!-- TODO: Remove this when we switch to fluent theme. -->
  <Style Selector="ToggleButton.PasswordBoxRevealButton:checked /template/ Path#PART_GlyphElement_Reveal">
    <Setter Property="IsVisible" Value="False" />
  </Style>

  <Style Selector="TextBox.monospaceFont /template/ TextPresenter#PART_TextPresenter">
    <Setter Property="TextBlock.FontFamily" Value="{StaticResource MonospacedFont}" />
    <Setter Property="TextBlock.FontSize" Value="14" />
  </Style>
  <Style Selector="TextBlock.monospaceFont">
    <Setter Property="TextBlock.FontFamily" Value="{StaticResource MonospacedFont}" />
    <Setter Property="TextBlock.FontSize" Value="14" />
  </Style>
  <Style Selector=":is(TextBox).Transparent">
    <Setter Property="Background" Value="Transparent" /> 
  </Style>
  <Style Selector="TextBox.largeHeader">
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="FontSize" Value="18" />
    <Setter Property="Margin" Value="0,0,0,25" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="AcceptsReturn" Value="True" />
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>
  <Style Selector="TextBox.selectableTextBlock">
    <Setter Property="Padding" Value="0" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="IsReadOnly" Value="True" />
  </Style>
  <Style Selector=":is(TextBox):pointerover /template/ Border#border">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighBrush}" />
  </Style>
  <Style Selector=":is(TextBox):focus /template/ Border#border">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighBrush}" />
  </Style>
  <Style Selector=":is(TextBox):error /template/ Border#border">
    <Setter Property="BorderBrush" Value="{Binding $parent.(DataValidationErrors.Errors), Converter={StaticResource ErrorDescriptorToBorderColorConverter}}" />
  </Style>
  <Style Selector="Control.docTabContainer">
    <Setter Property="Margin" Value="20" />
  </Style>
  <Style Selector="controls|GroupBox.docTabContainer">
    <Setter Property="TextBlock.FontSize" Value="40" />
    <Setter Property="Margin" Value="10" />
  </Style>
  <Style Selector="Grid.content">
    <Setter Property="TextBlock.FontSize" Value="14" />
  </Style>
  <Style Selector="TextBlock.hyperLink">
    <Setter Property="TextBlock.Foreground" Value="{DynamicResource HyperLinkForeground}" />
  </Style>
  <Style Selector="TextBlock.errorMessage">
    <Setter Property="TextBlock.Foreground" Value="{DynamicResource ErrorBrush}" />
  </Style>
  <Style Selector="TextBlock.warningMessage">
    <Setter Property="TextBlock.Foreground" Value="{DynamicResource WarningMessageForeground}" />
  </Style>
  <Style Selector="Button.activeHyperLink">
    <Setter Property="TextBlock.Foreground" Value="{DynamicResource HyperLinkForeground}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Background" Value="Transparent" />
  </Style>
  <Style Selector="dock|DockToolChrome:active /template/ Grid#PART_Grip">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
  </Style>
  <Style Selector="Button.chromeButton">
    <Setter Property="IsVisible" Value="False" />
  </Style>
  <Style Selector="ListBox">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderLowBrush}" />
  </Style>
  <Style Selector="ListBoxItem">
    <Setter Property="Padding" Value="5 0" />
  </Style>
  <Style Selector="TextBox">
    <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighBrush}" />
  </Style>

  <Style Selector="DataValidationErrors">
    <Setter Property="Template">
      <ControlTemplate>
        <DockPanel LastChildFill="True">
          <ContentControl
            DockPanel.Dock="Bottom"
            ContentTemplate="{TemplateBinding ErrorTemplate}"
            DataContext="{TemplateBinding Owner}"
            Content="{Binding (DataValidationErrors.Errors)}"
            IsVisible="{Binding (DataValidationErrors.HasErrors)}" />
          <ContentPresenter
            Name="PART_ContentPresenter"
            Background="{TemplateBinding Background}"
            BorderThickness="{TemplateBinding BorderThickness}"
            ContentTemplate="{TemplateBinding ContentTemplate}"
            Content="{TemplateBinding Content}"
            Padding="{TemplateBinding Padding}" />
        </DockPanel>
      </ControlTemplate>
    </Setter>
    <Setter Property="ErrorTemplate">
      <DataTemplate>
        <ItemsControl Items="{Binding}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding Message}" Foreground="{Binding Severity, Converter={StaticResource ErrorSeverityColorConverter}}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </DataTemplate>
    </Setter>
  </Style>

  <Style Selector="ListBoxItem:pointerover Grid Expander /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ThemeControlHighlightMidBrush}" />
  </Style>
  <Style Selector="ListBoxItem:pointerover Grid Expander:expanded /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
  </Style>
  <Style Selector="ListBoxItem:selected:pointerover Grid Expander /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ApplicationAccentBrushLow}" />
  </Style>
  <Style Selector="ListBoxItem:selected:pointerover Grid Expander /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ThemeHighlightBrush}" />
  </Style>
  <Style Selector="ListBoxItem:selected Grid Expander /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ApplicationAccentBrushLow}" />
  </Style>
  <Style Selector="ListBoxItem:selected:pointerover Grid Expander:expanded /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
  </Style>
  <Style Selector="ListBoxItem:selected Grid Expander:expanded /template/ ToggleButton#PART_toggle /template/ Border > Grid > Border">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
  </Style>

  <!-- Dock doesn't have feature to disable resizing. Make the Splitters that allow resize
       not visible to hit testing, in order to disable them.-->
  <Style Selector="dock|ProportionalStackPanelSplitter">
    <Setter Property="IsHitTestVisible" Value="False" />
  </Style>

  <Style Selector="shell|MetroWindow.ToolWindow /template/ Button#minimiseButton">
    <Setter Property="IsVisible" Value="False" />
  </Style>
  <Style Selector="shell|MetroWindow.ToolWindow /template/ Button#restoreButton">
    <Setter Property="IsVisible" Value="False" />
  </Style>
  <Style Selector="shell|MetroWindow.ToolWindow /template/ Image">
    <Setter Property="IsVisible" Value="False" />
  </Style>
  <Style Selector="shell|MetroWindow.ToolWindow /template/ Border > VisualLayerManager > Grid > Grid > Grid > Grid > TextBlock">
    <Setter Property="IsVisible" Value="False" />
  </Style>
  <Style Selector="shell|MetroWindow.ToolWindow /template/ DockPanel#titlebar > ContentControl">
    <Setter Property="IsVisible" Value="False" />
  </Style>
</Styles>
