<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui">
  <controls:GroupBox Title="{Binding Title}" BorderThickness="0" Classes="docTabContainer">
    <Grid Classes="content" Margin="10">
      <controls:ExtendedTextBox Text="{Binding TermsAndConditions}" Classes="selectableTextBlock" TextWrapping="Wrap" AcceptsReturn="True" />
    </Grid>
  </controls:GroupBox>
</UserControl>
