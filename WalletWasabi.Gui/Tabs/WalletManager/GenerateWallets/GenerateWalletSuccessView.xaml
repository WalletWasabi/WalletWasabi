<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui"
             x:Class="WalletWasabi.Gui.Tabs.WalletManager.GenerateWallets.GenerateWalletSuccessView">
  <StackPanel Margin="20" Spacing="10">
    <TextBlock Text="Write down these Recovery Words &amp; Your Password" FontWeight="Bold" />
    <Border Padding="20" Margin="20 0" HorizontalAlignment="Center">
      <ItemsControl Items="{Binding MnemonicWords}">
        <ItemsControl.Styles>
          <Style Selector="TextBlock">
            <Setter Property="TextBlock.FontFamily" Value="{StaticResource HandWrittenFont}" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="Margin" Value="40 10" />
            <Setter Property="Height" Value="32" />
          </Style>
        </ItemsControl.Styles>
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel MaxHeight="220" Orientation="Vertical" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </Border>
    <TextBlock Text="Your Password" Margin="0 0 0 20" FontFamily="{StaticResource HandWrittenFont}" HorizontalAlignment="Center" FontSize="22" />
    <TextBlock Text="You need BOTH the Recovery Words AND the Password to recover your wallet." />    
    <TextBlock Text="Heads Up! You always need the password to spend your bitcoin. Use a secure password and back it up."/>
    <Button Content="Find out more about password best practises here." Classes="activeHyperLink" Command="{Binding PasswordBestPracticesCommand}" HorizontalAlignment="Left"  />

    <CheckBox Margin="0 20 0 0" HorizontalAlignment="Center" IsChecked="{Binding IsConfirmed}" Content="I have written down my password and recovery words." />
    <Button Margin="0 10 0 0" HorizontalAlignment="Center" Content="Continue" Command="{Binding ConfirmCommand}" />
  </StackPanel>
</UserControl>
