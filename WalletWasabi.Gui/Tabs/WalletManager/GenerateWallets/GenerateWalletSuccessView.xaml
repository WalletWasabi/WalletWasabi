<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui"
             x:Class="WalletWasabi.Gui.Tabs.WalletManager.GenerateWallets.GenerateWalletSuccessView">
  <StackPanel Margin="20" Spacing="10">
    <TextBlock Text="Write down these Recovery Words!" FontWeight="Bold" />
    <Border Padding="20" Margin="20 0 20 20" HorizontalAlignment="Center">
      <ItemsControl Items="{Binding MnemonicWords}">
        <ItemsControl.Styles>
          <Style Selector="TextBlock">
            <Setter Property="TextBlock.FontFamily" Value="{StaticResource HandWrittenFont}" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="Margin" Value="40 10" />
            <Setter Property="Height" Value="32" />
          </Style>
        </ItemsControl.Styles>
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel MaxHeight="220" Orientation="Vertical" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </Border>
    <TextBlock Text="You can recover your wallet on any computer with" />
    <TextBlock Text="- your Recovery Words AND" />
    <TextBlock Text="- your Password." />
    <TextBlock Text="The order is IMPORTANT so include the numbers." />
    <TextBlock Text="You will not be able to see the recovery words again." />
    <TextBlock Text="Only click the button below when you have written them down." />
    <Button Margin="0 10 0 0" HorizontalAlignment="Center" Content="I wrote down my Recovery Words!" Command="{Binding ConfirmCommand}" />
  </StackPanel>
</UserControl>
