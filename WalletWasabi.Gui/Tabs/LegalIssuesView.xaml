<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:controls="clr-namespace:WalletWasabi.Gui.Controls;assembly=WalletWasabi.Gui">
  <controls:GroupBox Title="{Binding Title}" BorderThickness="0" Classes="docTabContainer">
    <Grid Classes="content" Margin="0 10 0 0">
      <controls:ExtendedTextBox Text="{Binding LegalIssues}" Classes="selectableTextBlock" TextWrapping="Wrap" AcceptsReturn="True" />
    </Grid>
  </controls:GroupBox>
</UserControl>
