jobs:
- job: Linux
  pool:
    vmImage: 'ubuntu-latest'
  steps:
  - script: "foo=$(for x in $(git diff --numstat master); do echo $x; done | awk '/^[0-9]+$/' | paste -s -d+ -)"
  - script: "echo $(( $foo ))"
