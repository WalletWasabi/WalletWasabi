<Styles xmlns="https://github.com/avaloniaui">

  <Style Selector="ContentArea :is(Control).title TextBlock">
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>

  <Style Selector="ContentArea :is(TextBlock).title">
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="FontWeight" Value="Bold" />
  </Style>

  <Style Selector="ContentArea :is(Control).caption TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
    <Setter Property="FontSize" Value="{DynamicResource FontSizeH7}" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="Opacity" Value="0.6" />
  </Style>

  <Style Selector="ContentArea :is(TextBlock).caption">
    <Setter Property="Foreground" Value="{DynamicResource TextControlForeground}" />
    <Setter Property="FontSize" Value="{DynamicResource FontSizeH7}" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="Opacity" Value="0.6" />
  </Style>

</Styles>
