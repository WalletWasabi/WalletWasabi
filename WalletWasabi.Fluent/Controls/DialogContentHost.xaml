<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="clr-namespace:WalletWasabi.Fluent.Controls">
  <Design.PreviewWith> 
  </Design.PreviewWith>
  <Styles.Resources>
  </Styles.Resources>
 <Style Selector="c|DialogContentHost">
    <Setter Property="Opacity" Value="0" />
    <Setter Property="Transitions">
      <Transitions>
        <TransformOperationsTransition Property="RenderTransform"  Duration="0:0:1" Easing="0.1,0.9,0.2,1.0"  />
        <DoubleTransition Property="Opacity" Duration="0:0:1" Easing="0.1,0.9,0.2,1.0" />
      </Transitions>
    </Setter> 
    <Setter Property="Template">
      <ControlTemplate>
        <Panel>
          <Panel Background="#3A3A3A" Name="Overlay" IsHitTestVisible="False" />
          <Border VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="5" BoxShadow="4 11 30 1 #90000000" Background="#212121" >
            <ContentPresenter Content="{TemplateBinding Content}"/>
          </Border>
        </Panel>
      </ControlTemplate>
    </Setter>
  </Style>
  <Style Selector="c|DialogContentHost:open">
    <Setter Property="Opacity" Value="0.8"/>
  </Style>
  <Style Selector="c|DialogContentHost:not(:open)">
    <Setter Property="Opacity" Value="0"/>      
  </Style>
</Styles>