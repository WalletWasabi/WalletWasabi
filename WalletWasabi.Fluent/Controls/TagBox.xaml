<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="clr-namespace:WalletWasabi.Fluent.Controls"

        xmlns:vmt="clr-namespace:WalletWasabi.Fluent.ViewModels.TagsBox">

    <Style Selector="c|TagBox">
        <Setter Property="Template">
            <ControlTemplate>
                <StackPanel x:DataType="vmt:TagBoxViewModel">
                    <ItemsControl Items="{CompiledBinding CombinedContent}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>
            </ControlTemplate>
        </Setter>
    </Style>

    <Style Selector="c|TagBox /template/ AutoCompleteBox">
        <Setter Property="Opacity" Value="0.5" />
    </Style>

</Styles>