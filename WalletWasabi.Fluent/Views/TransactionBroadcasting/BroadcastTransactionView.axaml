<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:c="using:WalletWasabi.Fluent.Controls"
             xmlns:converters="using:WalletWasabi.Fluent.Converters"
             xmlns:transactionBroadcasting="using:WalletWasabi.Fluent.ViewModels.TransactionBroadcasting"
             mc:Ignorable="d" d:DesignWidth="428" d:DesignHeight="371"
             x:DataType="transactionBroadcasting:BroadcastTransactionViewModel"
             x:CompileBindings="True"
             x:Class="WalletWasabi.Fluent.Views.TransactionBroadcasting.BroadcastTransactionView">
  <c:ContentArea Title="{Binding Title}"
                 EnableCancel="{Binding EnableCancel}"
                 EnableBack="{Binding EnableBack}"
                 EnableNext="True" NextContent="Broadcast"
                 IsBusy="{Binding IsBusy}">
    <StackPanel Spacing="20">
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Transaction ID"
                     Content="{Binding TransactionId, FallbackValue=ab550cbedcacc9e4d1d6d37b1dc54eee0cax7b70c4e0beeac2d0915353b70ae0}" />
      <Separator />
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Input count"
                     Content="{Binding InputCount, FallbackValue=1}" />
      <Separator />
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Output count"
                     Content="{Binding OutputCount, FallbackValue=1}" />
      <Separator />
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Input amount"
                     Content="{Binding TotalInputValue, Converter={x:Static converters:MoneyConverters.MoneyToString}, StringFormat={}{0} BTC,FallbackValue=0.00011234}" />
      <Separator />
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Input amount"
                     Content="{Binding TotalOutputValue, Converter={x:Static converters:MoneyConverters.MoneyToString}, StringFormat={}{0} BTC,FallbackValue=0.00011234}" />
      <Separator />
      <c:PreviewItem Icon="{StaticResource btc_logo}"
                     Text="Fee"
                     Content="{Binding NetworkFee, Converter={x:Static converters:MoneyConverters.MoneyToString}, StringFormat={}{0} BTC,FallbackValue=0.00011234}" />
    </StackPanel>
  </c:ContentArea>
</UserControl>
