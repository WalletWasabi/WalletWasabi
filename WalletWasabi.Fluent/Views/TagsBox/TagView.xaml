<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WalletWasabi.Fluent.ViewModels.TagsBox"
             mc:Ignorable="d"
             x:Class="WalletWasabi.Fluent.Views.TagsBox.TagView">
    <Panel>
        <Border BorderBrush="{DynamicResource TagsBoxControlForeground}"
                BorderThickness="1"
                CornerRadius="4"
                Margin="4"
                VerticalAlignment="Center">
            <TextBlock x:DataType="vm:TagViewModel"
                       Text="{CompiledBinding Tag}"
                       Margin="8,4" />
        </Border> 
    </Panel>
</UserControl>